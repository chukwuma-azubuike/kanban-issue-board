import { IssuePriority, IssueSeverity, IssueStatus } from '../types';

export const issuePriorities: Array<IssuePriority> = ['high', 'medium', 'low'];
export const issueStatuses: Array<IssueStatus> = ['Backlog', 'Done', 'In Progress'];
export const issueSeverities: Array<IssueSeverity> = [1, 2, 3, 4, 5];
